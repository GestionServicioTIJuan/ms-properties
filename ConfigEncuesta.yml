 # HTTP (Tomcat) port
server:
  port: 8585
  
#Configuracion de acceso a base de datos
spring:
  datasource:
    url: jdbc:mysql://10.1.2.35:3306/herramientas
    username: jarodrig
    password: jarodrig
    driverClassName: com.mysql.jdbc.Driver

#Scripts de gestion para encuestas
query:
  encuesta:
    lista: SELECT * FROM herramientas.encuestas;
    registra: INSERT INTO herramientas.encuestas VALUES (NULL,?,?,?,?,?,?,?,?);
  preguntas:
    listaPreguntas: SELECT * FROM herramientas.preguntas;
    registraPreguntas: INSERT INTO herramientas.preguntas VALUES(?,?,?,?);
    registraRespuestas: INSERT INTO `herramientas`.`respuestas` VALUES (NULL,?,FALSE,?);
